DEBUG = True

ALLOWED_HOSTS = ['localhost', '127.0.0.1']

DATABASES = {
    'default': {
        'ENGINE': 'django.contrib.gis.db.backends.postgis',
        'NAME': 'mosaic',
        'USER': 'mosaic',
        'PASSWORD': 'mosaic'
    },
}
